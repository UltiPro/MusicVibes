<Page x:Class="MusicVibes.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="860"

      xmlns:language="clr-namespace:MusicVibes.Properties.Langs"

      DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Background="{DynamicResource WhiteSpecial}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderColor1}" Background="{StaticResource BackgroundColor2}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top">

                <Label FontSize="24" FontWeight="Bold" Foreground="{DynamicResource FontColor1}" xml:space="preserve" Content="{x:Static language:Lang.Settings}"></Label>

            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="55" Background="{DynamicResource WhiteSpecial}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border CornerRadius="15" Background="{DynamicResource BackgroundColor1}" Margin="0,0,40,0">
                 <StackPanel Grid.Column="0"  >
                    <Label FontSize="18" FontWeight="Bold" Foreground="{DynamicResource FontColor1}" Margin="0,0,0,10" Content="Language" VerticalAlignment="Center" HorizontalAlignment="Center"/> 
                    
                        <DockPanel Margin="10,10,100,10">
                            <Button Content="EN" Width="32" Height="32" Margin="0,0,0,0" Click="LanguageButton_En" />
                        </DockPanel>

                        <DockPanel Margin="100,100,10,10">
                            <Button Content="PL" Width="32" Height="32" Margin="0,0,0,0" Click="LanguageButton_Pl" />
                        </DockPanel>
                                          
                </StackPanel>   
            </Border>
            

            <StackPanel Grid.Column="1" Margin="0,0,40,0" Background="{DynamicResource BackgroundColor1}">
                <TextBlock FontSize="18" FontWeight="Bold" Foreground="{DynamicResource FontColor1}" Margin="0,0,0,10">Theme Mode</TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Light" Width="32" Height="32" Margin="0,0,10,0" Click="ThemeButton_Light" />
                    <Button Content="Dark" Width="32" Height="32" Margin="0,0,10,0" Click="ThemeButton_Dark" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="0,0,40,0" Background="{DynamicResource BackgroundColor1}">
                <TextBlock  FontSize="18" FontWeight="Bold" Foreground="{DynamicResource FontColor1}" Margin="0,0,0,10">About Us</TextBlock>
                <TextBlock>
            Wersja aplikacji: 1.0.0
            Data: 22.06.2023
            Autorzy:
            Autor1
            Autor2
            Autor3
                </TextBlock>
            </StackPanel>
        </Grid>


        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderColor1}" Background="{StaticResource BackgroundColor2}">
            <DockPanel VerticalAlignment="Center">
                <!-- Add your player control buttons and volume controls here -->
            </DockPanel>
        </Border>
    </Grid>
</Page>
